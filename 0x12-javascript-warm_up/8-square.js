#!/usr/bin/node
//script that prints a square
let square = process.argv[2];
if (isNaN(x)) {
	console.log('Missing size');
} else {
	for (let i = 0; i < parseInt(x); i++) {
		console.log('X'.repeat(parseInt(square)));
	}
}
